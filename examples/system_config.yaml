usersim_system:
  model:
    provider: "openai"
    model_name: "gpt-4"
    
  hyperparameters:
    temperature: 0.7
    top_p: 0.95
    max_tokens: 1024
    
  defaults:
    max_turns: 20
    timeout_seconds: 300
    
  logging:
    level: "info"
